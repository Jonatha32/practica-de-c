#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <unistd.h>
#include "shell.h"
/**
 *
 */
int *_getenv(char **env)
{
  unsigned int i = 0;
  while (env[i] != NULL)
    {
      printf("%s\n", env[i]);
      i++;
    }
  return 0;
}
